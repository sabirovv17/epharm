-- Чек: какие промо-акции фармацевт заявил при загрузке (выбор в пикере мобилки).
-- Это ДЕКЛАРАЦИЯ фармацевта (claim), а не источник истины по бонусу — бонус
-- по-прежнему определяет сверка (pending-бонус POSM + лог Стандарт-Н + Excel).
-- Поле нужно модератору для контекста: «фармацевт утверждает, что в чеке акции X, Y».
-- Храним как CSV id кампаний (pr_*), ограничение длины — защита от мусора.
ALTER TABLE receipts ADD COLUMN claimed_promo_ids VARCHAR(512);
