-- V020 — полностью убираем OCR из сверки чеков.
--
-- Решение (2026-06-09): источники истины сверки = лог Стандарт-Н + Excel-выгрузка +
-- ручная модерация. Фото чека остаётся как доказательство для модератора, QR — под будущий
-- ОФД. Распознавание изображения (OCR) и его «уверенность» (ocr_score) больше не используются:
-- авто-одобрение происходит только при подтверждении двумя источниками (decideFromSources).
--
-- Поэтому колонка ocr_score удаляется. OcrService/MockOcrService удалены из кода.

ALTER TABLE receipts DROP COLUMN ocr_score;
