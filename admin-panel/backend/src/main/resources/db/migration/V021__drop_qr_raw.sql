-- V021 — полностью убираем ОФД из сверки чеков.
--
-- ОФД-верификации (онлайн-проверки фискального чека по QR) не будет ни сейчас, ни в будущем.
-- Источники истины сверки — ТОЛЬКО лог Стандарт-Н (программа на C#) + Excel-выгрузка,
-- плюс ручная модерация в админ-панели. Фискальный QR с чека не считывается и не хранится.
-- Поэтому колонка qr_raw удаляется (вслед за ocr_score в V020).
ALTER TABLE receipts DROP COLUMN qr_raw;
