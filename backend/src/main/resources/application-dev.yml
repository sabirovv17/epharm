spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/epharm
    username: epharm
    password: epharm_dev
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    kz.epharm: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
