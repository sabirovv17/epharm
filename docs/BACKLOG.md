# Бэклог

Актуальный список: что проверить дополнительно и что осталось до качественного завершения.
Обновлено: 2026-07-21. Строки удалять по мере выполнения; крупные решения — в notes-файлы.

## Статус системы (последняя полная проверка 2026-07-02)

| Слой                | Тесты                                                                      | Прод                          |
| ------------------- | -------------------------------------------------------------------------- | ----------------------------- |
| Backend (Kotlin)    | 347/347 ✅ (полный suite; OOM локальной JVM на 40-й мин — не код, см. П-1) | healthy, все контейнеры Up    |
| Админ-фронт (React) | 370/370 ✅ (39 файлов)                                                     | отдаётся, 401-гварды работают |
| Мобилка (Flutter)   | analyze 0 issues, 98/98 ✅                                                 | APK в MinIO, iOS на iPhone    |
| POSM (C#)           | ручное на VM                                                               | v1.0.18 prod-зип готов        |
| SMS (p1sms)         | юнит 4/4 ✅; ключ валиден; прод шлёт (status=sent)                         | доставка в роуминге — П-2     |

## П — Проверки работоспособности (сначала это)

- [ ] **П-1. Heap для тестовой JVM backend** — полный suite падает по OOM после ~40 классов
      (Spring кэширует контексты). Добавить в `build.gradle.kts` `test { maxHeapSize = "4g" }`
      (или forkEvery=10). Пока обход: гонять таргетно по пакетам.
- [ ] **П-2. Доставка SMS в роуминге** — p1sms принимает (id 1185747646/1185747660, sent),
      но до KZ-симки в роуминге не дошло. Ждём от Амира статусы доставки из ЛК p1sms +
      список зарегистрированных sender'ов → при необходимости переключить P1SMS_CHANNEL=char + P1SMS_SENDER (только .env.prod, без пересборки). Дотестить вход на номере,
      находящемся В Казахстане.
- [ ] **П-3. Первый холодный запрос каталога ~4.4с** (Medusa + пустой кэш, TTL 300с).
      Для «без лагов»: прогрев кэша на старте бэка или увеличить TTL + фоновый рефреш.
- [ ] **П-4. /api/mobile/pharmacies отдаёт 401 без токена** — подтвердить, что мобилка зовёт
      его только после входа (иначе экран аптек пустой у анонима).
- [ ] **П-5. CI на GitHub** — проверить, что последние коммиты ветки feat/mobile-backend
      зелёные (required checks), перед мержем в main.
- [ ] **П-6. POSM v1.0.44 на Ауэзова 134** — установить новый PROD ZIP одним
      `setup-autostart.bat` и пройти критерии из `12-posm-v1.0.44-firebird-auth.md`: авторизованный активный чек из
      Firebird, Ivatherm -> Borjomi, удаление триггера, клиентский экран, продавец, presence и ребут.
- [ ] **П-8. Внешний HTTPS gateway** — IT должен установить правильный `*.inkar.kz` сертификат и
      исправить Host/SNI routing: на 2026-07-21 gateway отдаёт просроченный `*.inteq.kz` и 404, а
      `http://epharm.inkar.kz:8060/api/health` возвращает 200. До исправления проверить, разрешён ли
      исходящий `8060` в каждой аптеке.
- [ ] **П-7. E2E вход по SMS на iPhone** — после решения П-2: номер → SMS → код → вход →
      кнопка «Отправить ещё раз» (кулдаун 60с, ошибка 429 читаемая).

## З — Завершение проекта (качество/фичи)

- [x] **Безопасное хранение продавца** — POSM передаёт id/ФИО Standard-N, backend сохраняет
      исходные значения отдельно от доверенного внутреннего профиля, а дашборд показывает даже
      несопоставленного продавца. Мобильные смены не используются.
- [x] **З-1. Стандарт-Н: источник живого чека** — подтверждены `SESSIONS` + `WORKSTATIONS`,
      `DOCS.STATUS=0/DOC_TYPE=3/AUDIT_ID=session` и строки `DOC_DETAIL_ACTIVE`; v1.0.43 читает только
      локальную кассовую сессию и автоматически берет сервер/path/login из `options.ini`.
- [ ] **З-2. Активный фармацевт в Стандарт-Н** — завести оператора с ФИО в АРМ Менеджер,
      войти в АРМ Кассир и подтвердить появление в логе/БД; затем настроить явное сопоставление
      внешнего Standard-N USER_ID с внутренним pharmacist_id.
- [x] **З-3. POSM: pharmacyId Ауэзова 134** — пакет использует
      `sloc_01KSAHYDSE5MVP79KGJSH2F2SK`; фактическое появление онлайн проверяется в П-6.
- [ ] **З-4. Второй экран (реклама)** — при подключении 2-го монитора: активный плейлист
      в разделе «Экраны» под нужную аптеку (логика в POSM уже готова).
- [ ] **З-5. Device key per-касса (Stage 4)** — сейчас общий POSM_DEVICE_KEY; для масштаба —
      ключ на устройство + выдача при онбординге.
- [ ] **З-6. SMS-провайдер: наблюдаемость** — webhookUrl p1sms для статусов доставки
      (delivered/undelivered) → журнал в админке; алерт при массовых недоставках.
- [ ] **З-7. Мобильные доделки** (старые #138/#139): QR-ОФД чеков, prod-readiness чеклист
      мобильного бэка (`docs/RELEASE-CHECKLIST.md`).
- [ ] **З-8. Витрины релиза** — APK подпись release-ключом (сейчас debug-key), iOS TestFlight
      вместо WiFi-установки, версия/чейнджлог.
- [ ] **З-9. Бэкапы** — регулярный `tools/pg-backup.sh` по крону на сервере + проверка
      восстановления; бэкап MinIO (медиа + apk).
- [ ] **З-10. Мониторинг прода** — сейчас только ручной health; минимум: uptime-пинг на
      /api/health + алерт в телегу/почту.

## Как пользоваться

Брать задачи сверху вниз внутри секции: П — блокирует уверенность в системе, З — путь к
завершению. Выполнил — вычеркни здесь и, если решение нетривиальное, зафиксируй в
`admin-panel/claude-admin-notes.md` / `docs/claude-notes.md`.
