# Historical Design Tokens

The original handoff tokens in this folder are superseded.

Use:

- `_reference/design-tokens.md` for current mobile design tokens;
- `admin-panel/design-tokens-admin.md` for current admin design tokens.

This file remains as a pointer so older references do not look authoritative.
