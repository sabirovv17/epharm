# Historical Profile Pages Reference

This folder documents the original HTML prototype profile sub-pages.

Current implementation is Flutter:

```text
lib/features/profile_pages/
├── data/
└── presentation/
```

Current pages:

- FAQ;
- instruction;
- cooperation;
- terms;
- privacy;
- shared scaffold/widgets.

Use `_reference/design-tokens.md` and Flutter code for current visual rules. The old `src/screens/*`
paths mentioned in the prototype are not production paths.
